name: smooth_button_example
description: Example app for smooth_button.
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  smooth_button: any

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
